# WordStat 开发日志

本文档记录 WordStat 单词统计工具的开发过程，包括每日完成的功能、测试内容和主要改进。

## 2026-08-22：项目初始化与需求设计

### 完成内容

- 创建项目并编写 README 文档。
- 明确单词统计规则：
  - 单词仅由英文字母组成。
  - 非英文字母均视为分隔符。
  - 统计时不区分大小写。
- 将程序处理过程拆分为文件输入、字符读取、单词识别、大小写处理、统计、频率计算和结果输出等步骤。
- 创建开发日志和 `.gitignore`。
- 添加第一份文本测试文件。

### 关联提交

- `87901fb`：创建 README
- `f6e0391`：补充程序处理流程
- `2abe195`：创建开发日志
- `6215208`：添加 `.gitignore`
- `f1652aa`：添加测试文本

## 2026-08-24：实现文件读取

### 完成内容

- 调整 `.gitignore` 规则。
- 添加空文件测试用例。
- 实现用户输入文件路径。
- 使用文件指针打开文本文件。
- 逐字符读取文件内容。
- 处理文件打开失败、读取结束和文件关闭。
- 使用 `int` 保存字符读取结果，以便正确区分普通字符与 `EOF`。

### 测试内容

- 正常文本文件。
- 空文件。
- 不存在的文件路径。
- 文件读取至 `EOF`。
- 文件资源是否正常关闭。

### 关联提交

- `64d4b0b`：修改 `.gitignore` 规则
- `8651cd5`：增加空文件测试用例
- `9b2a4c8`：实现文件内容读取

## 2026-08-25：实现字符分类与单词提取

### 完成内容

- 判断读取到的字符是否为英文字母。
- 将英文字母以外的字符作为分隔符。
- 使用字符数组保存连续出现的英文字母。
- 遇到分隔符时结束并输出当前单词。
- 处理文件以字母结尾时的最后一个单词。
- 增加单词最大长度限制，防止字符数组越界。
- 完善文件路径长度和输入边界处理。

### 测试内容

- 大写和小写英文字母。
- 空格、数字、标点和换行符。
- `hello-world` 等包含分隔符的文本。
- 文件末尾有无换行符。
- 49、50 和 51 个连续字母的边界情况。

### 关联提交

- `d3169c3`：增加测试文本
- `b17ae8e`：实现英文字母与其他字符的分类
- `34b343a`：增加单词提取测试文本
- `5509cf6`：实现单词提取
- `65efc01`：修复边界情况

## 2026-08-26：实现大小写转换与单词总数统计

### 完成内容

- 将提取出的英文字母统一转换为小写。
- 实现不区分大小写的单词处理。
- 统计文件中的单词总数。
- 正确处理连续分隔符和文件结尾，避免单词漏计或重复计数。

### 测试内容

- `Hello`、`HELLO`、`hello` 和 `HeLLo` 的转换结果。
- 空文件和只包含分隔符的文件。
- 单个单词和多个单词。
- 文件末尾有无换行符。

### 关联提交

- `00708c0`：增加大小写转换测试文本
- `e6a6a41`：实现字符大小写转换
- `65e1596`：实现单词总数统计

## 2026-08-27：实现不同单词及出现次数统计

### 完成内容

- 定义单词统计结构体，保存单词及其出现次数。
- 使用固定数组保存不同单词的统计结果。
- 查找单词是否已经存在：
  - 已存在时增加出现次数。
  - 不存在时新增统计项。
- 设置最多统计 1000 个不同单词的限制。
- 添加单词统计测试数据。

### 关联提交

- `b82f6dc`：增加单词统计测试文本
- `979bd83`：完成单词统计结构体和统计数组

## 2026-08-28：实现频率计算与次数排序

### 完成内容

- 根据单词出现次数和单词总数计算频率。
- 使用浮点数除法保留频率的小数部分。
- 避免空文件发生除以零错误。
- 实现单词统计结果排序。
- 按单词出现次数从高到低排列。
- 增加排序测试文件。

### 关联提交

- `0072ee2`：实现单词频率计算和输出
- `7f11d20`：增加排序测试文件
- `b1e87b7`：实现单词统计结果降序排序

## 2026-08-31：完善排序并实现前 N 名输出

### 完成内容

- 完善排序规则。
- 出现次数不同时，按次数从高到低排列。
- 出现次数相同时，按单词字母顺序从小到大排列。
- 将排序比较逻辑整理为独立判断，减少重复的交换代码。
- 增加包含 100 个不同单词的测试文本，用于验证统计数量和排序结果。
- 实现由用户输入 N，并输出出现次数最多的前 N 个单词。
- 对 N 的基本范围进行检查，避免输出时访问统计数组边界之外的元素。

### 关联提交

- `1751bf4`：实现相同次数时按字母顺序排序
- `fe20770`：增加 100 个不同单词的测试文本
- `c9c831d`：实现用户输入前 N 个单词统计功能
- `f2e7242`：初始化并整理开发日志

## 2026-09-01：拆分功能模块并补充测试

### 完成内容

- 将单词统计相关代码从 `main.c` 拆分到独立的 `word_stats` 模块。
- 创建 `word_stats.h`，集中声明单词结构体、统计结构体、容量限制和对外函数。
- 创建 `word_stats.c`，集中实现单词计数、频率计算、排序和前 N 名输出。
- 调整统计函数的返回值，减少成功与失败状态之间的歧义。
- 优化统计结果和错误场景的提示信息。
- 对单词统计逻辑进行整理，完善统计数量的更新时机和上限处理。
- 增加约 1200 个单词的测试文本，用于测试较长文本的统计行为。
- 增加超长文件路径测试文件，用于验证路径长度限制。
- 将文件路径获取、逐字符读取、单词提取、字母判断和小写转换拆分到独立的 `file_reader` 模块。
- 创建 `file_reader.h` 和 `file_reader.c`，进一步减少 `main.c` 中的具体处理逻辑。
- 使 `main.c` 主要负责组织文件读取、单词统计和结果输出的执行流程。

### 代码结构

```text
src/
├── main.c
├── file_reader/
│   ├── file_reader.c
│   └── file_reader.h
└── word_stats/
    ├── word_stats.c
    └── word_stats.h
```

### 关联提交

- `54dcd72`：将单词统计功能拆分为独立模块
- `3653119`：修改统计错误返回值，避免状态歧义
- `3eface9`：优化结果展示信息
- `8deb74d`：优化单词统计功能
- `82b0840`：增加约 1200 个单词的测试文本
- `215fa60`：优化程序提示语
- `5a02945`：增加长路径测试文件
- `6a61df1`：将文件读取功能拆分为独立模块

## 当前进度

目前已经完成单词统计工具的主要功能：

- 读取文本文件。
- 识别并提取英文单词。
- 不区分大小写统计。
- 统计单词总数。
- 统计不同单词及其出现次数。
- 计算单词频率。
- 按次数和字母顺序排序。
- 输出出现次数最多的前 N 个单词。
- 将文件读取和单词统计拆分为独立模块。

后续将继续完成：

- 完善用户输入验证。
- 整理最终输出。
- 清理调试信息。
- 继续完善模块接口和错误处理。
- 完成拆分后的回归测试。
